// oracle.cpp
#include <iostream>
#include <vector>
#include <cmath>

using Matrix = std::vector<std::vector<double>>;

Matrix defect_operator(const Matrix& K, const std::vector<int>& partition) {
    // Compute D_Π = (I-P)KP
    // Return as JSON
}
